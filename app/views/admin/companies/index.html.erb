<%= render partial: 'search', locals: { companies: @companies } %>
<div class="row">
  <div class="col s12">
    <div class="card">
      <div class="card-content">
        <div class="table-responsive m-b-20">
          <table class="">
            <thead>
              <%= render 'thead' %>
            </thead>
            <tbody>
              <%= render partial: 'tbody', locals: { companies: @companies } %>
            </tbody>
          </table>
        </div>
        <%= paginate @companies %>
      </div>
    </div>
  </div>
</div>
